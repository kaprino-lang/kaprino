//
// Simple calculator
//

#let x = 0.0 (<- R).
#let y = 0.0 (<- R).

#read x.
#read y.

#let addans = x + y (<- R).
#let subans = x - y (<- R).
#let mulans = x * y (<- R).
#let divans = x / y (<- R).
#let powans = x ^ y (<- R).

#print x, " + ", y, " = ", addans.
#print x, " - ", y, " = ", subans.
#print x, " * ", y, " = ", mulans.
#print x, " / ", y, " = ", divans.
#print x, " ^ ", y, " = ", powans.
